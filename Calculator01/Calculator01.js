// deno-lint-ignore-file no-unused-vars no-inner-declarations no-var
/*
let usr_inp = prompt("Enter the number math expression");
usr_inp = usr_inp.replace("x", "*");
function change_sign(str = "") {
	if (str.startsWith("-")) {
		return str.substring(1);
	} else {
		return "-" + str;
	}
}

function percentage(str = "") {
	let value = null;
	if (str.includes("/")) {
		str = eval(str);
		value = parseFloat(str) * 100;
		return value + "%";
	}
	value = parseFloat(str) / 100;
	return value + "%";
}

let result = "";
try {
	result = eval(usr_inp);
} catch (error) {
	console.error("error");
	console.warn("Error");
}
*/
